import { createContext } from "react";

//Recebe os dados do usuário em GetUser
export const ContextProps = createContext({} as any)

//Altera o estado após requ isição em GetUser
export const ContextGetUser = createContext({} as any)